#summary tomcat6 servlet container


=Table of Contents= 
<wiki:toc max_depth="2"/>

= Apache Tomcat 6 Servlet Container =

= Install =

== Ubuntu ==
 * 1. sudo apt-get install tomcat6 tomcat6-common tomcat6-admin tomcat6-user tomcat6-docs tomcat6-examples
 * 2. add tomcat manager user. For more information [http://tomcat.apache.org/tomcat-6.0-doc/manager-howto.html Manager App HOW-TO] 
 * 3. sudo /etc/init.d/tomcat6 restart
 * 4. goto http://localhost:8080

 apt-get
{{{
 sudo apt-get install tomcat6 tomcat6-common tomcat6-admin tomcat6-user tomcat6-docs tomcat6-examples
}}}

 add manager role, and user in /etc/tomcat6/tomcat-users.xml
{{{
 <role rolename="manager"/>
 <user username="tomcat" password="s3cret" roles="manager"/>
}}}

 tomcat restart
{{{
 sudo /etc/init.d/tomcat6 restart
}}}

== Microsoft Windows ==
 * 1. goto windows installer - [http://tomcat.apache.org/download-60.cgi download]
 * 2. follow instructions - record username and port
 * 3. goto http://locahost:8080
 * 4. goto Tomcat Manager Link and login as the user 

= Quick Localhost Links =
 * [http://localhost:8080/manager/html localhost manager]
 * [http://localhost:8080/host-manager/html localhost admin]
 * [http://localhost:8080/docs/ localhost docs]

= Domain.tld/Archive.war =
 Naming your archive.war will determine your servlet context. Or could say archive.war represents the folder_name compared to html /folder/page.jsp
 * for domian.tld/context = domain.tld/archive, or archive.war deployed
 * for domain.tld/(root) = domain.tld/ROOT, or ROOT.war deployed