#summary test out wiki stuff

---------------
{{{
  List<String> list = ['a', 'b', 'c'];

  // good
  for (final item in list) {
    print(item);
  }

  // bad
  for (var item in list) {
    print(item);
  }
}}}
----------------