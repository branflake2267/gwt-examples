#summary test out wiki stuff

---------------
{{{
class Location {
  final String initailizeMe;
  final List<int> ids;
  num lat, lng; // instance variables
  
  Location(this.lat, this.lng) : ids = [], initailizeMe = "initialized"; // final variable initializers
}

void main() {
  var waikiki = new Location(21.271488, -157.822806);
  
  print(waikiki.lat);
  print(waikiki.lng);
}
}}}
----------------