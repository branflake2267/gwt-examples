#summary test out wiki stuff

---------------
{{{
@Entity
    public T get(Key<T> key) {
        ofy().clear();
        return ofy().get(key);
    }
    
    public T get(Long id) {
        ofy().clear();
        return ofy().get(clazz, id);
    }
}}}
----------------