#summary test out wiki stuff

<g:plusone size="medium"></g:plusone>


Test this out

{{{
     private void setValidDates(ShowRangeEvent<Date> dateRangeEvent) {
        Date start = dateRangeEvent.getStart();
        Date end = dateRangeEvent.getEnd();

        Integer daysBetween = CalendarUtil.getDaysBetween(start, end);

        for (int i = 0; i < daysBetween; i++) {
            Date date = new Date(start.getTime());
            CalendarUtil.addDaysToDate(date, i);
            setDatePickable(date);
        }
    }
}}}